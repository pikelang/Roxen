<font size=-1>
<select onChange='document.forms[0].submit();' 
        name=mailbox_id>
  <list-mailboxes>
    <option 
<if variable="mailbox_id is #id#">
     selected
</if>
     value=#id#>#name# (unread: #unread#/#mail#)
  </list-mailboxes>
</select>
</font> <gbutton name=list_unread value="List unread"><gbutton name=list_all value="List all">

