<?xml version="1.0" encoding="iso-8859-1"?>
<locale version="1.0"/>
<project>mod_secure_fs</project>
<language>eng</language>
<file>../modules/compat/secure_fs.pike</file>

<str id="1">
<o>File systems: Secure file system</o>
<t></t>
</str>

<str id="2">
<o>This is a file system module that allows for more fine-grained control
over the Roxen's built-in module security. Instead of just having security
pattern for the whole module it is possible to create several patterns.
Glob patterns are used to decide which parts of the file system each
pattern affects.

&lt;p&gt;The module also supports form based authentication. The same type of
access control can be achieved, in a different way, by using the
&lt;i&gt;.htaccess support&lt;/i&gt; module.
</o>
<t></t>
</str>

<str id="3">
<o>Security patterns</o>
<t></t>
</str>

<str id="4">
<o>Use form authentication</o>
<t></t>
</str>

<str id="5">
<o>New authentication will be required if no page has been requested within this time, in seconds.</o>
<t></t>
</str>

<str id="6">
<o>Form authentication page.</o>
<t></t>
</str>

<str id="7">
<o>Should contain an form with input fields named &lt;i&gt;httpuser&lt;/i&gt; and &lt;i&gt;httppass&lt;/i&gt;. The string $File will be replaced with the URL to the current page being accessed and $Me with the URL to the site.</o>
<t></t>
</str>
