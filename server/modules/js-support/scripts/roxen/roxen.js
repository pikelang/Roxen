/**
 * The ROXEN object.
 * @module roxen
 * @title ROXEN Global
 */

if (typeof ROXEN === "undefined" || !ROXEN) {
  /**
   * The ROXEN global namespace object. 
   * @class ROXEN
   * @static
   */
  var ROXEN = {};
}
